pipeline:
  name: Harness Take Home
  identifier: Harness_Take_Home
  projectIdentifier: default_project
  orgIdentifier: default
  tags: {}
  properties:
    ci:
      codebase:
        connectorRef: Git_source_repository
        repoName: harness-th
        build: <+input>
  stages:
    - stage:
        name: Build
        identifier: Build
        description: ""
        type: CI
        spec:
          cloneCodebase: true
          platform:
            os: Linux
            arch: Amd64
          runtime:
            type: Cloud
            spec: {}
          execution:
            steps:
              - step:
                  type: Run
                  name: Build the project
                  identifier: Run_1
                  spec:
                    connectorRef: account.harnessImage
                    image: harness-cicd-image:latest
                    shell: Sh
                    command: docker build -t gcr.io/harness-take-home/harness-cicd-image:latest .
